# Auto-Lurk-Message
A lightweight userscript that automatically sends periodic chat messages while you lurk on Twitch — complete with a top‑navigation toggle, live countdown timer, color‑coded status indicator, and stream‑awareness logic so it only runs when the stream is actually live.
